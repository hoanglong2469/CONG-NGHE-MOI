import {Service} from 'aws-sdk/lib/serviceb/service';
import {Presigner} from 'aws-sdk/lib/polly/presignerpresigner';
export class PollyCustomizations extends Service {
    static Presigner: typeof Presigner;
}