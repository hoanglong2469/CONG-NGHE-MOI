import {Credentials} from 'aws-sdk/lib/credentialsedentials';
import SSO = require('aws-sdk/clients/ssoso');
import { HTTPOptions } from 'aws-sdk/lib/config-basenfig-base';
export class SsoCredentials extends Credentials {
    /**
     * Creates a new SsoCredentials object.
     */
    constructor(options?: SsoCredentialsOptions);
}

interface SsoCredentialsOptions {
    httpOptions?: HTTPOptions,
    profile?: string;
    filename?: string;
    ssoClient?: SSO;
}
